# Dez App → FocusLock (MVP)

This repo contains the **FocusLock** MVP (Expo React Native + TypeScript).

## App location
- `focuslock/`

## Run it

```bash
cd focuslock
npm install
npm run ios
```

You can also run:

```bash
npm run android
npm run web
```


